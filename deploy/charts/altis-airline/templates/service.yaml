apiVersion: v1
kind: Service
metadata:
  name: {{ include "altis-airline.fullname" . }}
  labels:
    {{- include "altis-airline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "altis-airline.selectorLabels" . | nindent 4 }}
